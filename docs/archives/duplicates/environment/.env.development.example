# ============================================================================
# FARMERS MARKET PLATFORM - DEVELOPMENT ENVIRONMENT VARIABLES
# Copy this file to .env.local for local development
# ============================================================================

# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
NODE_ENV=development
PORT=3001
DEV_PORT=3001

# Public URLs (Development)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# ============================================================================
# DATABASE CONFIGURATION (PostgreSQL with PostGIS)
# ============================================================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=farmersmarket
POSTGRES_PORT=5432

# Prisma Database URLs (Development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/farmersmarket
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/farmersmarket

# For Docker Development (when running app in container)
# DATABASE_URL=postgresql://postgres:postgres@db:5432/farmersmarket
# DIRECT_URL=postgresql://postgres:postgres@db:5432/farmersmarket

# ============================================================================
# REDIS CACHE CONFIGURATION
# ============================================================================
REDIS_PASSWORD=devpassword
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://:devpassword@localhost:6379

# For Docker Development
# REDIS_HOST=redis
# REDIS_URL=redis://:devpassword@redis:6379

# ============================================================================
# AUTHENTICATION (NextAuth v5)
# ============================================================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=development-secret-key-minimum-32-characters-long-for-development
AUTH_TRUST_HOST=true

# ============================================================================
# OAUTH PROVIDERS (Optional - Use test credentials)
# ============================================================================

# Google OAuth (Development)
GOOGLE_CLIENT_ID=your-dev-google-client-id
GOOGLE_CLIENT_SECRET=your-dev-google-client-secret

# GitHub OAuth (Development)
GITHUB_CLIENT_ID=your-dev-github-client-id
GITHUB_CLIENT_SECRET=your-dev-github-client-secret

# ============================================================================
# STRIPE PAYMENT INTEGRATION (Test Mode)
# ============================================================================
# Get test keys from: https://dashboard.stripe.com/test/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_test_publishable_key
STRIPE_SECRET_KEY=sk_test_your_test_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_test_webhook_secret

# ============================================================================
# EMAIL CONFIGURATION (MailHog for Development)
# ============================================================================
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=dev@farmersmarket.local

# For Docker Development
# SMTP_HOST=mailhog
# SMTP_PORT=1025

# ============================================================================
# AI SERVICES (Optional - Development)
# ============================================================================
ENABLE_AI_FEATURES=true

# OpenAI API (Use test keys or low limits)
OPENAI_API_KEY=sk-your-openai-test-api-key

# Perplexity AI API
PERPLEXITY_API_KEY=pplx-your-perplexity-test-api-key
PERPLEXITY_DEFAULT_MODEL=SONAR_PRO

# ============================================================================
# MONITORING & OBSERVABILITY (Disabled in Development)
# ============================================================================
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
ENABLE_TRACING=true
APPLICATIONINSIGHTS_CONNECTION_STRING=

# ============================================================================
# FILE UPLOAD CONFIGURATION
# ============================================================================
UPLOAD_MAX_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/webp,image/gif,application/pdf

# ============================================================================
# FEATURE FLAGS (Development)
# ============================================================================
ENABLE_ANALYTICS=false
ENABLE_SPEED_INSIGHTS=false
ENABLE_EXPERIMENTAL_FEATURES=true

# ============================================================================
# DEVELOPMENT TOOLS
# ============================================================================
# Enable detailed logging
DEBUG=*
LOG_LEVEL=debug

# Hot reload configuration
WATCHPACK_POLLING=false
CHOKIDAR_USEPOLLING=false

# Disable telemetry
NEXT_TELEMETRY_DISABLED=1

# HP OMEN Optimization (12 threads, 64GB RAM)
NODE_OPTIONS=--max-old-space-size=16384 --max-semi-space-size=512

# ============================================================================
# TESTING
# ============================================================================
# Test database (optional separate DB for tests)
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/farmersmarket_test

# ============================================================================
# DOCKER COMPOSE PORTS (When using docker-compose.dev.yml)
# ============================================================================
# App: http://localhost:3000
# Prisma Studio: http://localhost:5555
# MailHog UI: http://localhost:8025
# Adminer: http://localhost:8080
# Redis Commander: http://localhost:8081
# PgAdmin: http://localhost:8082 (with --profile advanced)
# Node Debugger: localhost:9229

# ============================================================================
# DEVELOPMENT NOTES
# ============================================================================
# 1. Copy this file to .env.local for local development
# 2. Update database URLs if using Docker (change localhost to service name)
# 3. MailHog catches all emails - view at http://localhost:8025
# 4. Use Stripe test mode keys only
# 5. Enable experimental features for testing new functionality
# 6. Prisma Studio available at http://localhost:5555
# 7. All emails are caught by MailHog (no real emails sent)
# 8. Redis data persists between restarts via Docker volumes
# 9. Database data persists between restarts via Docker volumes
# 10. Use npm run dev for local development without Docker
# 11. Use docker-compose -f docker-compose.dev.yml up for full Docker dev
# ============================================================================
