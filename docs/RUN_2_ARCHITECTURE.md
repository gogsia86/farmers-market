# 🏗️ Run 2: Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    CLIENT LAYER (Browser)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Search Bar  │  │   Filters    │  │  Product     │          │
│  │   Component  │  │   Component  │  │  Grid        │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                  │                  │                   │
│         └──────────────────┴──────────────────┘                   │
│                            │                                      │
│                    ┌───────▼────────┐                            │
│                    │ State Manager  │                            │
│                    │ (React State)  │                            │
│                    └───────┬────────┘                            │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │   HTTP Request   │
                    │  (fetch/axios)   │
                    └────────┬─────────┘
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                    API LAYER (Next.js)                            │
├────────────────────────────┼──────────────────────────────────────┤
│                            │                                      │
│         ┌──────────────────▼──────────────────┐                  │
│         │  /api/search/products               │                  │
│         │  • GET  - Query param search        │                  │
│         │  • POST - Advanced body search      │                  │
│         └──────────────────┬──────────────────┘                  │
│                            │                                      │
│         ┌──────────────────▼──────────────────┐                  │
│         │  /api/search/suggestions            │                  │
│         │  • GET  - Autocomplete              │                  │
│         └──────────────────┬──────────────────┘                  │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │ Query Builder    │
                    │ (search.utils)   │
                    └────────┬─────────┘
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                  BUSINESS LOGIC LAYER                             │
├────────────────────────────┼──────────────────────────────────────┤
│                            │                                      │
│  ┌─────────────────────────▼────────────────────────┐            │
│  │        Search Utilities                           │            │
│  │  • buildProductSearchQuery()                      │            │
│  │  • calculatePaginationMeta()                      │            │
│  │  • parseSearchParams()                            │            │
│  │  • getCurrentSeason()                             │            │
│  │  • sanitizeSearchQuery()                          │            │
│  └─────────────────────────┬────────────────────────┘            │
│                            │                                      │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                    ┌────────▼─────────┐
                    │  Prisma Client   │
                    │  (database)      │
                    └────────┬─────────┘
                             │
┌────────────────────────────┼──────────────────────────────────────┐
│                   DATABASE LAYER (PostgreSQL)                     │
├────────────────────────────┼──────────────────────────────────────┤
│                            │                                      │
│  ┌─────────┐   ┌──────────▼───┐   ┌──────────┐   ┌──────────┐  │
│  │  Farm   │───│   Product    │───│ Category │   │  Images  │  │
│  │  Table  │   │   Table      │   │  Table   │   │  Table   │  │
│  └─────────┘   └──────────────┘   └──────────┘   └──────────┘  │
│                                                                   │
│  Indexes: name, price, categoryId, farmId, stockQuantity         │
│                                                                   │
└───────────────────────────────────────────────────────────────────┘
```

## Data Flow

### 1. Search Flow

```
User Input → Debounce (300ms) → State Update → URL Update → API Call
    ↓
Prisma Query Builder → Database Query → Results + Metadata
    ↓
JSON Response → State Update → UI Render
```

### 2. Filter Flow

```
User Selects Filter → Component State Update → Parent Callback
    ↓
Filters Merge → URL Parameters Update → API Request
    ↓
New Results → Re-render with Loading Skeleton → Display Products
```

### 3. Autocomplete Flow

```
User Types (2+ chars) → Debounced API Call → Parallel Queries
    ↓
[Products, Farms, Categories] → Format Suggestions → Display Dropdown
    ↓
User Selects → Navigate to Product/Farm/Search Page
```

## Component Hierarchy

```
SearchPage
├── SearchBar (Client)
│   ├── Input
│   ├── Suggestions Dropdown
│   └── Submit Button
│
├── SearchFilters (Client)
│   ├── Accordion
│   │   ├── Availability Filter
│   │   ├── Price Range Slider
│   │   ├── Category Checkboxes
│   │   ├── Farm Checkboxes
│   │   └── Special Filters (Organic, Seasonal)
│   └── Clear All Button
│
└── SearchResults
    ├── Loading State (Skeleton)
    │   └── ProductGridSkeleton
    │
    └── Results Display
        ├── Results Header
        ├── Product Grid
        │   └── ProductCard[]
        └── Pagination
```

## Type System Architecture

```
types/search.ts
├── Filters
│   ├── ProductSearchFilters
│   ├── FarmSearchFilters
│   └── ActiveFilters
│
├── Sorting
│   ├── ProductSortOption
│   └── FarmSortOption
│
├── Pagination
│   ├── PaginationMeta
│   └── PaginatedResponse<T>
│
├── Results
│   ├── SearchResult<T>
│   ├── SearchSuggestion
│   └── FilterOption
│
└── Agricultural
    ├── Season
    ├── SeasonalContext
    └── AgriculturalMetadata
```

## Performance Architecture

### Caching Strategy

```
┌─────────────────────────────────────────────┐
│  L1: Client State (React)                   │
│  TTL: Session                                │
│  Size: Unlimited                             │
└─────────────────┬───────────────────────────┘
                  │ Miss
┌─────────────────▼───────────────────────────┐
│  L2: Browser Cache (HTTP)                   │
│  TTL: 5 minutes                              │
│  Size: Browser limit                         │
└─────────────────┬───────────────────────────┘
                  │ Miss
┌─────────────────▼───────────────────────────┐
│  L3: API Response (Next.js)                 │
│  TTL: Dynamic                                │
│  Revalidation: On-demand                     │
└─────────────────┬───────────────────────────┘
                  │ Miss
┌─────────────────▼───────────────────────────┐
│  L4: Database (PostgreSQL)                  │
│  Indexes: name, price, category, farm       │
│  Query Optimization: Parallel execution      │
└─────────────────────────────────────────────┘
```

### Query Optimization

```
Single Request → Multiple Parallel Queries

Promise.all([
  database.product.findMany(query),     // Products
  database.product.count({ where }),     // Count
  database.category.findMany(...),       // Categories
  database.farm.findMany(...),           // Farms
  database.product.aggregate(...)        // Price stats
])

↓

All results return simultaneously
Response time: ~200ms (with indexes)
```

## Security Architecture

```
User Input
    ↓
┌───────────────────────┐
│ Input Sanitization    │
│ • Remove special chars│
│ • Length limit: 100   │
│ • Type validation     │
└─────────┬─────────────┘
          ↓
┌───────────────────────┐
│ Prisma Parameterized  │
│ Queries               │
│ • SQL injection safe  │
│ • Type-safe queries   │
└─────────┬─────────────┘
          ↓
┌───────────────────────┐
│ Database Execution    │
│ • Row-level security  │
│ • Read-only for search│
└───────────────────────┘
```

## Agricultural Consciousness Integration

```
Season Detection
    ↓
getCurrentSeason() → SPRING | SUMMER | FALL | WINTER
    ↓
getSeasonalContext(season) → Recommendations
    ↓
┌─────────────────────────────────────┐
│ Seasonal Metadata Injection         │
│ • Recommended categories            │
│ • Seasonal products                 │
│ • Biodynamic alignment              │
└─────────────────────────────────────┘
    ↓
Response includes agricultural consciousness
```

## State Management Architecture

```
URL Parameters (Source of Truth)
    ↓
parseSearchParams(searchParams)
    ↓
Local State (ProductSearchFilters)
    ↓
User Interaction (Filter Change)
    ↓
filtersToSearchParams(filters)
    ↓
URL Update (router.push)
    ↓
Triggers useEffect → API Call → Update Results
```

---

_Divine Agricultural Architecture_ 🌾🏗️✨
