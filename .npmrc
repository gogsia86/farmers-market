# HP OMEN Optimization - 64GB RAM, 12 threads, RTX 2070 8GB
# Optimized for maximum Node.js performance
# PLATFORM-AGNOSTIC: Works on Windows, Linux, and macOS

# Memory allocation (32GB for Node.js - half of 64GB RAM)
max-old-space-size=32768

# Thread pool size (use all 12 CPU threads)
uv-threadpool-size=12

# Additional Node.js optimizations
node-options=--max-old-space-size=32768 --max-semi-space-size=128

# Parallel operations - REMOVED script-shell=powershell for cross-platform compatibility
# Use platform's default shell (sh/bash on Linux, PowerShell on Windows)

# Network optimizations
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# Cache settings for faster installs
# Use prefer-offline instead of deprecated cache-min
prefer-offline=false

# Progress display
progress=true
loglevel=info
