# 🔴 REDIS CONFIGURATION
# Redis Cloud (Redis Labs) - EU Central Region
# Last Updated: January 12, 2026

# Redis Connection Details
REDIS_HOST=redis-18095.crce198.eu-central-1-3.ec2.cloud.redislabs.com
REDIS_PORT=18095
REDIS_PASSWORD=9MaIaT7jeI8L7cmS0PUDbTWI2RQ3o0kK
REDIS_DB=0
REDIS_KEY_PREFIX=fm:

# Full Redis URL (alternative to individual vars)
REDIS_URL=redis://default:9MaIaT7jeI8L7cmS0PUDbTWI2RQ3o0kK@redis-18095.crce198.eu-central-1-3.ec2.cloud.redislabs.com:18095

# Redis Cloud Configuration
REDIS_TLS_ENABLED=false
REDIS_USERNAME=default
REDIS_MAX_RETRIES=3
REDIS_CONNECT_TIMEOUT=10000

# Cache Configuration
CACHE_PROVIDER=redis
CACHE_DEFAULT_TTL=3600

# Note: Port 18095 does not require TLS for this Redis Cloud instance

# Rate Limiting (using Redis)
UPSTASH_REDIS_REST_URL=https://redis-18095.crce198.eu-central-1-3.ec2.cloud.redislabs.com:18095
UPSTASH_REDIS_REST_TOKEN=9MaIaT7jeI8L7cmS0PUDbTWI2RQ3o0kK

# Performance Tuning
REDIS_ENABLE_OFFLINE_QUEUE=true
REDIS_ENABLE_READY_CHECK=true
REDIS_MAX_RECONNECT_DELAY=3000
REDIS_RETRY_STRATEGY_ENABLED=true

# ============================================================================
# INSTRUCTIONS:
# ============================================================================
# 1. Copy this file to .env.local for local development:
#    cp .env.redis .env.local
#
# 2. Or add these variables to your existing .env.local
#
# 3. For production, add to Vercel environment variables:
#    - Go to Vercel dashboard > Project > Settings > Environment Variables
#    - Add each REDIS_* variable
#
# 4. Test connection:
#    npm run dev
#    Check logs for: "✅ Redis client connected"
#
# ============================================================================
